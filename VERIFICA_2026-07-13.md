# VERIFICA-CODE-MOD — 2026-07-13 (deploy @785 → @786)

## Esito: ⚠️ CLOBBER RILEVATO E RIPARATO → ✅ tutto riconciliato

## Fase 1 — Snapshot e riconciliazione
- **Situazione trovata**: produzione a **v4.27.x (@785)** da una sessione parallela; repo locale fermo a v4.25.22 (@778). Git locale pulito (baseline `06273146`).
- **Novità della linea v4.27** (altra sessione, adottate): arricchimento bandi via Claude Haiku (`enrichBandiRadarBatch`, daily 03:00, 50/run), batch **54 osservatori culturali internazionali** (`fontiAggiungiBatchOsservatori`), `apiScanTutto` compattato nel dispatcher, redesign Styles/Index/HomeView, `preview-home.html`, log `ocCronSetup` migliorato.
- 🔴 **Clobber**: il push dell'altra sessione (locale privo di `Discovery_v1.js`) lo aveva **cancellato dal remoto** → in produzione i bottoni "+ Podcast cultura / + Social / Audit" chiamavano funzioni **inesistenti**. Persi anche: 2 entry dispatcher (`podcastAuditMensile`, `discoveryAutoSeedOnce`), bottone unico "Attiva fonti", collasso no-concorsi in Lavoro cultura.
- **Riconciliazione eseguita**: adottati gli 11 file v4.27 → ripristinato `Discovery_v1.js` → ri-innestati i 4 pezzi @778. Nessuna perdita su nessuna delle due linee.

## Fase 2 — Controllo multilivello
| Check | Esito |
|---|---|
| Sintassi (`node --check`, tutti i .js) | ✅ 90+ file OK |
| Bilanciamento `<div>` Index.html | ✅ delta 0 |
| Integrità riferimenti (dispatcher + whitelist AdminTools + schedule base) | ✅ **53 funzioni referenziate, 0 mancanti** |

## Fasi 3-5 — Collaudo sciame / fonti / stress
Non eseguibili da questa postazione (richiedono runtime GAS con sessioni utente). Delegati ai meccanismi già attivi: QA notturna (23:00), report FONTI (08:00), agenti qualità. **Suggerito per il collaudo manuale**: pannello Strumenti → `Stato cron`, `Audit podcast/video`, self-test gate/lavoro/norme.

## Stato finale
- **Deploy @786 · v4.27.19** — produzione = HEAD editor = repo locale = GitHub (`18da2c00`)
- Email di sintesi: non inviabile da CLI; il report FONTI giornaliero (08:00) e la QA notturna coprono la notifica. Questo file è il documento di ripartenza.

## ⚠️ Lezione operativa (recidiva)
Due sessioni Claude parallele su cloni locali diversi si sovrascrivono a vicenda via `clasp push`. **Regola**: a inizio sessione fare sempre pull-diff (questa procedura); una sola sessione "proprietaria" del push per volta; il repo git è l'arbitro.

## Ripartenza — aperti
1. Mittente email (decisione canale, sospesa)
2. Collaudo pannello Strumenti post-@786 (i 3 bottoni podcast/social ora funzionano davvero)
3. Volumi Lavoro cultura / Opportunità da monitorare
