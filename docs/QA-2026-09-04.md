# QA Osservatorio Culturale — 4 settembre 2026

*Procedura `verifica-code-mod` completa. Baseline: repository `fix/collisioni-namespace`,
foglio produzione letto a oggi, feed GU e casella Gmail dal vivo. Nessuna scrittura su
produzione: solo letture e correzioni al codice.*

---

## Esito in una riga
Codice sano (114 file, 0 errori di sintassi, 0 collisioni di namespace); l'app serve
regolarmente; le fonti sono vive (60–95 contenuti/settimana). **Un solo blocco: 7 commit
pronti aspettano il deploy** — finché non parte, restano ferme le riparazioni di trend e
lavoro cultura.

## Fase 1 — Snapshot e baseline
- Snapshot git: HEAD `485bd4b`, tag `qa-2026-09-04`.
- 20 commit avanti rispetto alla baseline produzione (`sync/produzione-20260820`).
- **7 commit non ancora deployati** (dal deploy @988 del 25/08): mappa Europa, deploy.ps1,
  skill scout + doppia cinta + auto-applica, dossier newsletter, fix trend, fix lavoro.

## Fase 2 — Controllo multilivello del codice
| Controllo | Esito |
|---|---|
| Sintassi 114 file .js | 🟢 0 errori |
| Sintassi script Index.html/HomeView | 🟢 ok |
| Collisioni di funzioni tra file | 🟢 nessuna (era la causa del blocco newsletter di agosto — resta pulito) |
| `var` globali duplicate | 🟡 3 innocue: `FONTI_AGENTI_SHEET`, `OC_MATRIX_CONTACTS_SHEET`, `OC_MATRIX_RESPONSES_SHEET` — stesso valore letterale in due file, duplicazione voluta per indipendenza dei moduli |
| Chiamate `google.script.run` senza funzione server | 🟢 nessuna reale (i 2 apparenti — `getItem`, `toDataURL` — sono `localStorage.getItem` e `canvas.toDataURL`, falsi positivi) |
| Chiamate a funzioni private (finale `_`) dal client | 🟢 nessuna |

## Fase 3 — Collaudo live
- App deployata: **HTTP 200**, pagina completa (~2 MB) servita.
- Endpoint trend (`?trend=proponi`): risponde correttamente.
- Feed GU 4ª Serie: raggiungibile, 74 concorsi nel numero corrente.
- *Nota tecnica*: il collaudo browser-driven degli endpoint `google.script.run` non è
  eseguibile da questo ambiente (il proxy chiude il websocket verso script.google.com);
  la verifica è stata fatta a livello di codice + probe HTTP diretti + lettura del foglio.

## Fase 4 — Fonti
- **Vive**: attività datata costante — W31→W35 tra 64 e 95 contenuti/settimana, W36 in corso.
  Nessun crollo → gli scanner girano.
- **Scout**: Routine cloud del lunedì eseguita con successo (31/08, 06:17→06:23).
- **Candidate** (foglio FontiCandidate): 117 totali — 97 in valutazione, 8 approvate, 12 scartate.
- *Registro fonti attive*: l'export testuale del foglio non è parsabile per-riga (celle con
  JSON multi-linea in `extras_json`); la salute delle fonti è comunque confermata dal flusso
  di contenuti sopra e dalla policy di ritiro automatico (`fasRetryFontiSilenti`) già attiva.

## Fase 5 — Anomalie e riparazioni
Corrette in questa sessione (già committate, in attesa di deploy):
1. **Lavoro cultura fermo da 18 scansioni** → verifica GU in ordine di priorità culturale
   (comuni/fondazioni prima del CNR). Vedi `docs/SALUTE-SISTEMA-2026-09-04.md`.
2. **Falsi "lavoro"** (bandi CER Puglia "selezione di proposte") → classificatore distingue
   selezione di cose vs di persone. 8/8 collaudo.
3. **Trend fermo da 3 settimane** → parsing date corretto (commit 27/08).

Nessuna nuova anomalia critica emersa dalle Fasi 2–4. Le 3 `var` duplicate sono lasciate come
sono (identiche e volute); nessuna riparazione a rischio in sospeso.

## Cosa resta a te
1. **Deploy** `.\deploy.ps1 -Descrizione "v4.33"` — sblocca trend + lavoro + scout.
2. Post-deploy: collaudo trend via `?trend=proponi&rinnova=sinopia2026`.
3. Decisioni newsletter aperte (dossier): coda DigestQueue, mittente unico profilati,
   fallback tematico.
