# Ripresa lavoro — Osservatorio Culturale / Sinopia

> Documento di handoff per riprendere in una **nuova sessione**. Sessione del 24/06/2026.
> Stato: **deploy @664 in produzione**, branch sincronizzato su GitHub. Tutto verde.

## ⚡ Aggiornamento 25/06 (mattina)
- ✅ **Punto 1 FATTO** (@663): login pubblico **ON di default** (via codice, nessuna azione manuale).
- ✅ **Punto 3 RISOLTO**: santini si attiva da solo al login (pending sbloccati + login ON); in alternativa Stato→attivo nella tabella utenti.
- ⚙️ **Punto 2 RESTA** (l'unica azione tua): eseguire `setupMasterTriggers()` da editor GAS per applicare lo schedule Opzione B (digest bozze domenica, niente invio auto lunedì). Verifica con `setupMasterDiagnostica()`. → `clasp run` non ha i permessi per eseguirlo da remoto.
- ✅ **Punto 8 FATTO** (@664): auto-registrazione invia **una sola email** (il magic-link, già di benvenuto); welcome separata soppressa nei flussi con magic-link.
- ✅ **Pulizia brand**: rimossi gli ultimi `duemilamusei` user-facing (outreach ROC + testo SVG card bando + link welcome).
- Test: 157 + 22 (coorti) + 12 (auth-flow) verdi.

---

## 1. Stato attuale (dati operativi)

| Voce | Valore |
|---|---|
| **Deploy attivo** | **@659** |
| **Deployment ID** (per `clasp deploy -i`) | `AKfycbyUpp_zM0I4vg3AKVXQKsvhwiKUHFP4YOURGjh5a05evdeEQpuOQIjakngeWyfIzVqs` |
| **URL produzione** | `https://script.google.com/macros/s/AKfycbyUpp.../exec` |
| **Branch git** | `feat/fonti-feed-unificazione` (sincronizzato con `origin`) |
| **Repo GitHub** | `straccini/Osservatorio-Culturale` |
| **Brand / Titolare** | **Sinopia Srl** · Via del Raku 10, 06053 **Deruta (PG)** · P.IVA 03766660546 · PEC sinopiaconsulting@pec.it |
| **Email contatto pubblica** | **sinopiaconsulting@gmail.com** (login/identità admin restano `s.straccini@gmail.com`) |

---

## 2. Cosa è stato fatto oggi (per area)

### A. Privacy & GDPR (allineato)
- Nuova pagina interna **Privacy & GDPR** (`#page-privacy`, accessibile agli ospiti) con informativa completa redatta sui trattamenti reali: aggregazione automatica + testi elaborati da **AI**, dati minimi (nome/cognome/email), **niente dati sensibili**, **niente cessione a terzi**, **niente trasferimenti extra-UE** (Google art. 28 + garanzie artt. 44-49), DPO non dovuto, diritti, cookie, reclamo Garante. Framing **fase test/beta**.
- **Footer globale** (Chi siamo · Contatti sinopiaconsulting@gmail.com · Privacy & GDPR · LinkedIn).
- Tutti i link "Informativa completa"/"Privacy policy"/consensi → **pagina interna** (incluso l'ultimo residuo in HomeView).
- Codice a supporto: `forgetMyData` esteso a `UtmLog`; `purgeContattiObsoleti()` (retention, dry-run, admin).
- Doc aggiornato: `docs/REGISTRO_TRATTAMENTI_SINOPIA.md` (coerente con l'informativa).

### B. Rebranding / coerenza (Sinopia · Deruta · email brand)
- `Fano (PU)` → `Deruta (PG)` nelle stringhe di **sede aziendale** (tenuti i riferimenti geografici: Urbino-Pesaro-Fano, Teatro Fortuna, Comune di Fano).
- `s.straccini@gmail.com` → `sinopiaconsulting@gmail.com` in contatti/outreach/CTA user-facing (TENUTI: login/identità admin, super-admin check, OC_ADMIN_DEFAULT_, notifiche admin, sender newsletter, test admin).
- `Duemilamusei` → `Sinopia` ovunque (outreach ROC, KB, CRM, seed, schema, questionario Matrix, commenti). TENUTI: path file `KB_Duemilamusei.md`, var interna `OC_MATRIX_DUEMILAMUSEI_FOOTER`, note di versione storiche.

### C. SEO
- Webapp (Index.html `<head>` + Codice.js doGet): meta description/robots/author/theme-color, **Open Graph + Twitter Card**, **JSON-LD @graph** (Organization=**Sinopia Srl** + WebApplication + WebSite), canonical.
- Landing Netlify (`landing-netlify/index.html`): redirect auto **0,8s → 5s** con countdown + **annullamento alla prima interazione** (i bot non vengono mai reindirizzati); contrasto **WCAG AA**; numeri **2×2 / 1 colonna** su mobile; schema.org Organization; sezione contenuti indicizzabile.
- **og:image**: i meta puntano a `https://app.sinopiaconsulting.it/og-cover.png`. Creato l'artwork sorgente `landing-netlify/og-cover.svg` (1200×630). **PNG ancora da generare** (vedi §4).

### D. Digest — Opzione B (revisione prima dell'invio)
- `cronGenerateDigestWeekly` spostato da **martedì 06:00 → domenica 18:00** (genera bozze + Telegram).
- `sendDigestAuto2coorti` **NON più schedulato**: invio a tutte le coorti **manuale** dal pannello (pulsante **"Invia digest settimanale"** con anteprima dry-run + doppio clic di conferma).
- ⚠️ Richiede `setupMasterTriggers()` da editor GAS per applicare il nuovo schedule (vedi §4).

### E. Profilati / coorti digest (bug grosso risolto)
- **3 coorti**: A lettori · B Matrix/lead · C profilati. Precedenza **B > C > A**.
- **Bug root**: la coorte B prendeva **chiunque avesse una sessione** (ogni login) → 18 Matrix/lead, 0 lettori, 0 profilati. **Fix**: B solo veri compilatori Matrix (`matrix_completato`); **hardening**: anche chi è in `ContactsMatrix` con `response_id`.
- Coorte C ora rilevata **dal foglio ProfiliPro** (non dalla MailingList).
- Coorte A (generalisti) ora include anche gli **Utenti registrati** con OptInDigest (non solo MailingList).
- Scheda admin rinominata "Digest profilati" → **"Digest Matrix"**; aggiunta la **vera** scheda "Digest profilati" (ProfiloPro) con prova d'invio + **🔎 Verifica rilevamento profilati** (diagnostico `getDiagnosiCoorti`: riconcilia profili-nel-foglio vs coorti).
- Colonna **"Profilo"** nella tabella utenti admin.

### F. Login / Registrazione — modello LIGHT (semplificato)
- **Ingresso unificato a UN solo gesto**: il box a 2 tab "Accedi/Registrati" → **un form** (email + nome facoltativo + consenso → "Entra"). Login = registrazione.
- `loginConEmail`: email non registrata → **auto-registra lettore attivo** + magic-link (param `nome` opzionale).
- `requestAccess`: **niente più pending/approvazione** per i lettori → auto-attivazione lettore + magic-link.
- Pending **non più bloccati** al login (risolto **catch-22**: prima un pending non poteva loggarsi → non riceveva il link → non si attivava mai). Sospesi/rifiutati restano bloccati.
- **Bug risolto**: `createSessione` accetta solo *source fidate* → le chiamate light usavano source non valide → **magic-link non inviato**. Ora usano `'registrazione'`.
- Accesso automatico (magic-link → rientro nel profilo) **verificato 12/12**.

---

## 3. Procedure operative (come lavorare)

### Deploy (procedura sicura)
```bash
clasp push                                  # carica i file locali su GAS
clasp deploy -i AKfycbyUpp_zM0I4vg3AKVXQKsvhwiKUHFP4YOURGjh5a05evdeEQpuOQIjakngeWyfIzVqs -d "vXYZ — descrizione"
```
⚠️ **MAI** `clasp deploy` senza `-i` (creerebbe un NUOVO deployment = nuovo URL = utenti persi).

### Test (eseguire prima di ogni deploy)
```bash
node tools/test-fixes.js          # 157 test (gate FAS, getEditoria, ecc.)
node tools/simula-profili.js      # 22 scenari (coorti A/B/C + registrazione) — codice reale
node tools/test-auth-flow.js      # 12 scenari (magic-link, login light, pending) — codice reale
node tools/check-inline-js.js Index.html   # JS inline (il blocco JSON-LD dà 1 falso positivo, ok)
node --check <File>.js             # sintassi
```

### Git / backup
```bash
git add <file specifici>          # NON usare 'git add -A' (rischio di inglobare .netlify/node_modules)
git commit -m "..."
git push origin feat/fonti-feed-unificazione
```
- `tools/`, `docs/`, `*.md`, `landing-netlify/`, `.netlify/` sono in `.claspignore` (non vanno su GAS).

---

## 4. DA FARE (priorità per il lancio di fine mese)

### 🔴 Azioni di configurazione (solo tu, da editor GAS / pannello)
1. **Login pubblico ON**: il flag `OC_PUBLIC_LOGIN_ENABLED` deve essere **attivo** (Impostazioni → Login pubblico → Attiva, oppure `abilitaLoginPubblico`). Senza, i lettori non entrano col login manuale (le chiavi magic-link già esistenti funzionano comunque).
2. **Trigger Opzione B**: eseguire `setupMasterTriggers()` da editor GAS per applicare il nuovo schedule (digest bozze domenica 18:00, niente invio automatico lunedì). Verificare con `setupMasterDiagnostica()`.
3. **santini (g.santini@amat.marche.it, pending)**: ora si attiva da solo al primo login; in alternativa metterlo `attivo` nella tabella utenti.

### 🟠 Asset / pubblicazioni
4. **og-cover.png**: esportare `landing-netlify/og-cover.svg` → PNG **1200×630** chiamato `og-cover.png` nella root di `landing-netlify` (un convertitore online va bene). Serve per le anteprime social.
5. **Ripubblicare la landing su Netlify** (le modifiche a `landing-netlify/` NON passano dal deploy GAS).
6. **URL X/Twitter**: il footer ha solo LinkedIn. Quando hai l'handle/URL, aggiungerlo (footer Index.html ~riga 2590 + eventuale `twitter:site`).

### 🟡 Sviluppi previsti
7. **Campi contatto futuri (telefono / WhatsApp / Telegram)**: predisposizione PRONTA (gate pulito + `loginConEmail` con param nome = stesso schema). Per attivarli: (a) colonne `Telefono/WhatsApp/Telegram/CanaleContatto` nel foglio Utenti/ProfiliPro; (b) param opzionali a `loginConEmail`/`_autoRegisterUser_`/`registraUtente`; (c) input nel profilo (meglio del gate, per tenere l'ingresso leggero) + preferenza "come contattarti". *Additivo, zero rischio.*
8. **2 email all'auto-registrazione**: oggi partono benvenuto (`_autoRegisterUser_` → `sendWelcomeEmail`) **+** magic-link (`createSessione`). Da consolidare in **una sola** (link d'accesso dentro il benvenuto).
9. **Validazione legale** dell'informativa privacy (revisione di un professionista) prima di considerarla definitiva — è una bozza solida ma in fase test.

### 🟢 Opzionale / igiene
- Unificare `tools/simula-profili.js` e `tools/test-auth-flow.js` nella suite principale.
- Codice morto nel gate: `OC._gateTab` / `OC._gateDoReg` non più usati (innocui, rimovibili).

---

## 5. Decisioni chiave prese (per non rimetterle in discussione)
- **Brand**: Sinopia ovunque (rebrand totale, track record incluso). Duemilamusei resta solo in path-file e note storiche.
- **Sede pubblica**: Deruta (PG) ovunque.
- **Email**: brand `sinopiaconsulting@gmail.com` ovunque (tranne identità di login admin).
- **Modello accesso LIGHT**: login = registrazione, nessuna approvazione per i lettori, magic-link permanente, pending mai bloccanti. Approvazione solo per ruoli elevati (liste admin/editor).
- **Digest**: Opzione B (bozze automatiche, invio manuale dopo revisione).
- **Dedup email**: stessa identica email = blocco "già registrato"; stessa persona con email diverse = consentito.

---

## 6. File principali toccati (riferimento rapido)
- **Auth/sessioni**: `Auth.js` (_autoRegisterUser_, requestAccess), `Sessioni_v1.js` (loginConEmail, validaSessione, createSessione)
- **Digest/coorti**: `Digest_routing.js` (getDigestRecipientsByCohort), `Matrix_digest.js` (generateDigestForDims, testDigestProfilatoUI), `ProfiloPro_v1.js` (saveProfilo, getProfiliProList, getDiagnosiCoorti)
- **Frontend**: `Index.html` (gate `_gateOspite`/`_gateDoLogin`, pagina privacy, footer, scheda Digest profilati, tabella utenti), `HomeView.html`, `Styles.html`
- **SEO/legale**: `Codice.js` (doGet meta), `landing-netlify/index.html`, `landing-netlify/og-cover.svg`, `docs/REGISTRO_TRATTAMENTI_SINOPIA.md`
- **Test/tool** (claspignored): `tools/test-fixes.js`, `tools/simula-profili.js`, `tools/test-auth-flow.js`, `tools/check-inline-js.js`

---

*Ultimo deploy: @659 · branch sincronizzato · 157 + 22 + 12 test verdi.*
