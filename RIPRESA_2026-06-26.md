# RIPRESA — 27 giugno 2026

Sessione del 26/06 focalizzata su **salute fonti, bandi e automazioni**. Tutto deployato e su GitHub.

## Stato
- **Deploy attivo**: `@688` (URL invariato `AKfycbyUpp…/exec`)
- **Branch**: `feat/fonti-feed-unificazione` — git pulito, locale = remoto (`5831918f`)
- **Test**: 157 PASS · inline JS ok (solo falso positivo JSON-LD)

## Fatto oggi (deployato)
1. **Fonti morte (28)**: diagnosi `qaDiagnosiFontiMorte` → `qaApplicaFixFonti` (4 URL corretti verificati + 23 disattivazioni reversibili).
2. **Scanner news a batch**: `scanSourcesBatch` (cursore + budget 4,6 min, no timeout 6 min).
3. **Ingestione newsletter**: `NewsletterIngest.js` — `scanNewsletterGmail` legge etichetta Gmail `Osservatorio`, estrae, salva in Items. Etichette Gmail già create.
4. **Documento Word per Riccardo**: `Iscrizione_Newsletter_Osservatorio.docx` (11 fonti + istruzioni).
5. **QA notturna nativa GAS** (Binario B): `qaNotturnaGAS` + `qaFontiSilenti`.
6. **Skill FONTI giornaliera**: `FontiReport.js` — `fontiReportGiornaliero` (silenti + outage aggregatori TED/ANAC/UE + bandi senza link specifico) + `fontiAggiungiFeedVerificato` (cancello auto-add #2).
7. **Discovery curata**: `fontiAggiungiBatch` (6 fonti RSS verificate) + skill `scopri-fonti-notebooklm` (in Documents/Claude/Scheduled).
8. **Cron dispatcher**: `CronDispatcher.js` — `ocCronSetup`/`ocCronDispatch` consolida ~20 trigger in 1.
9. **Bandi**: `bandiPulisciVecchi`/`…Applica` (archivia solo scaduti — **applicato**: 418 archiviati, 1068 attivi) + filtro cultura `FAS_BDNCP_CULTURA_RX` allargato all'accessibilità.
10. **Sync lavori paralleli**: ApiConnettori.js, guest-locked page (Index.html), PubDiscovery_v1.js.

## Da fare (TUE azioni nell'editor GAS — `clasp run` non autorizzato)
Priorità alta → bassa:

1. **`fontiAggiungiBatch`** → aggiunge le 6 fonti verificate (Inexhibit, Europa Nostra, Hyperallergic, Digital Meets Culture, +2 da testare). [offerto, non ancora lanciato]
2. **`qaApplicaFixFonti(false)`** → applica i 4 fix URL + 23 disattivazioni (l'anteprima `(true)` mostrava tutto ok). [se non già applicato]
3. **Consolida trigger** (sei a 20/20): `qaListaTrigger()` → `ocCronSetup(true)` (dry-run) → `ocCronSetup(false)` (applica). ⚠️ Dopo NON rilanciare `setupMasterTriggers()`.
4. **Newsletter**: iscriviti alle fonti del doc Word → etichetta `Osservatorio` → `scanNewsletterAnteprima` → `scanNewsletterGmail` → (via dispatcher).
5. **Report fonti**: `fontiReportAnteprima` per vederlo → poi lo gestisce il dispatcher.

## Decisioni prese
- **Bandi**: solo **non scaduti** (archiviare solo gli scaduti, gli aperti restano sempre).
- **Resto delle fonti**: si lascia tutto com'è — "la ricchezza dell'Osservatorio è l'ampiezza di ciò che monitora".
- **ANAC**: l'OCP Data Registry (storico ≤ set 2025) è **inutile** per bandi nuovi → connettore OCP rimosso; si usa la pipeline live BDNCP+TED (`fasRunCompleto`).
- **Auto-add nuove fonti (#2)**: auto-iscrizione se passa i controlli (gate `fontiAggiungiFeedVerificato`).

## Prossimo filone (quando vuoi)
Discovery automatica via skill NotebookLM (`scopri-fonti-notebooklm`) + 2° lotto fonti (ICOM Italia, NEMO, Doppiozero, Tafter, ArtEconomy24, Europeana Pro, Creative Europe Desk).
